# moon-weblink 项目申报书

## 基本信息

- 项目名称：moon-weblink —— RFC 8288 / RFC 9264 链接与链接集工具库
- 参赛者：廖龙超
- 联系方式：以赛事报名系统登记信息为准
- GitHub 仓库：https://github.com/15614376790/moon-weblink
- 项目方向：MoonBit HTTP 协议基础库 / Web 基础设施
- 是否移植项目：否（原创实现，参考 RFC 规范与 IANA 公开数据）

## 项目简介

moon-weblink 是一个 MoonBit 库，按 RFC 8288（Web Linking）和 RFC 9264（Linkset）实现 HTTP Link 头及链接集（linkset）的解析、序列化、规范化、查询与审计，附带命令行工具 weblink-tool 和五个可运行示例。库的解析入口对任何输入都不 panic，超限或畸形输入一律返回带字节偏移的结构化错误。全部代码、文档、测试在 wasm、wasm-gc、js、native 四套 MoonBit 后端上编译运行通过。

## 项目方向与适用场景

HTTP 协议基础设施方向。典型场景：

- Web 框架与 HTTP 客户端：解析服务端 Link 响应头，处理 `rel="next"` 分页、`rel="preload"` 资源提示、`rel="canonical"` 等；
- 链接集发布与消费：按 RFC 9264 在 `application/linkset+json` 与 Link 头文本之间互转；
- 爬虫与 API 工具：从响应头提取关系、校验链接合法性、输出确定性规范化形式；
- 离线运行环境：内置 IANA 关系注册表快照，运行时零网络访问。

## 核心功能

- RFC 8288 link-value 解析与序列化：URI-Reference、引号串、扩展参数、大小写不敏感的参数名；规范化输出字节级稳定、幂等；
- RFC 8187 ext-value（`charset'lang'value`）解码与编码；
- RFC 3986 URI-Reference 解析与引用消解（merge + remove-dot-segments）；
- RFC 9264 Linkset：`application/linkset` 文本与 `application/linkset+json` 解析、序列化，模型层转换（header ↔ text ↔ json）；
- 离线 IANA Link Relation Types 注册表（134 条快照），关系查询与注册检查；
- 确定性审计：`rev` 废弃参数、未注册关系前缀、`title`/`title*` 并存、重复参数等检查项，错误模型 `(stage, kind)` 跨版本稳定；
- 资源限制预设（default / strict / permissive）；
- 命令行工具 weblink-tool：parse、validate、canonicalize、query、to/from-linkset-json、to-linkset-text、relation、audit、stats、version、help；
- 测试与门禁：147 个命名测试（140 库黑盒 + 7 CLI 白盒）、1200 个确定性属性用例、2504 个截断用例；43 步验证脚本（格式检查、接口检查、四目标严格 check/build/test、逐目标 CLI 冒烟、示例、行数预算、IANA 快照完整性与包清单）全部通过，并由 GitHub Actions 持续验证；
- 五个示例程序：parse_header、pagination、linkset_json、relation_query、audit_header。

## 原创性说明

本项目为原创实现，未移植任何现有代码库：

- 协议行为依据 RFC 8288、RFC 9264、RFC 8187、RFC 3986 规范自行设计实现，不复制任何既有实现的代码；
- 唯一引用的外部数据是 IANA Link Relation Types 注册表（https://www.iana.org/assignments/link-relations/ ）的公开数据快照，来源与校验信息记录在 `testdata/iana/SOURCE.json`，生成与完整性校验脚本（`scripts/import_iana_relations.py`、`scripts/verify_iana_snapshot.py`）随仓库提供；
- 项目许可证：Apache-2.0。

## 终审交付说明

- 最终提交版本：`0.1.1`；本次终审保持核心协议行为不变，补齐四目标严格验证、GitHub Actions 与发布文档；
- 有效代码量：6233 行（核心 3403 行、CLI 与示例 872 行、测试 1958 行）；
- 可复现质量数据：147 个测试、1200 个确定性属性用例、2504 个截断安全用例、134 条 IANA 关系记录；
- 交付渠道：终审代码已推送 GitHub，`0.1.1` 已发布为 Mooncakes 包 `15614376790/moon-weblink`。
